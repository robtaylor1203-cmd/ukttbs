{
  "name": "ukttbs",
  "version": "1.0.0",
  "description": "Static site hosted on **GitHub Pages**, with **Supabase** (Postgres + Auth) as the\r backend and **Stripe** for payments. No build step — plain HTML/CSS/JS.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "gsap": "^3.15.0",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "vite": "^8.0.9"
  }
}
