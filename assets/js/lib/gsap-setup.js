/**
 * Central GSAP + ScrollTrigger registration.
 * Every scene module imports { gsap, ScrollTrigger } from here so we
 * only resolve the CDN modules once.
 */

// Removed for redesign: GSAP and ScrollTrigger setup.
